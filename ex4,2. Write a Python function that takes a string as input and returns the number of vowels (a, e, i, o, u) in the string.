def count_vowels(text):
    vowels = "aeiouAEIOU"
    vowel_count = 0

    for char in text:
        if char in vowels:
            vowel_count += 1

    return vowel_count

text = input("Enter a string: ")
print("Number of vowels:", count_vowels(text))
